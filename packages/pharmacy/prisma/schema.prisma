generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("_TOPP_PHARMACY_DB_URL")
}

model User {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
