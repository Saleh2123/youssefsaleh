{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "check.eslint": { "dependsOn": ["^build", "build"] },
    "check.prettier": {},
    "check": { "dependsOn": ["check.eslint", "check.prettier"] }
  }
}
